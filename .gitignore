*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/

# Used by dotenv library to load environment variables.
# .env

# Ignore Byebug command history file.
.byebug_history

## Specific to RubyMotion:
.dat*
.repl_history
build/
*.bridgesupport
build-iPhoneOS/
build-iPhoneSimulator/

## Specific to RubyMotion (use of CocoaPods):
#
# We recommend against adding the Pods directory to your .gitignore. However
# you 
Of course, here is the complete code for a `.gitignore` file:

```
# Ignore all files with the .kpd extension
*.kpd

# Ignore the node_modules directory and all its contents
/node_modules

# Ignore log files
*.log

# Ignore compiled files and directories
dist/
build/

# Ignore sensitive data files (e.g. files containing passwords)
sensitive_file.txt

# Ignore all .pdf files
*.pdf
```

This code defines patterns for files and directories that you want to ignore when working with Git. Modify or add new lines according to your needs. Remember that the `.gitignore` file should be placed in the root directory of your project for Git to recognize it.
